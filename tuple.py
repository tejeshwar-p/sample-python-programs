# tuple is a type of ds. We can store multiple pieces of information.
coordinates = (4, 5)
# tuple is immutable. Once created tuple cannot be modified.
print(coordinates)
print(coordinates[0])
print(coordinates[1])

other_tuple = (2, 3, 4, 5)
print(other_tuple)

# tuple is generally used if data does not change.
